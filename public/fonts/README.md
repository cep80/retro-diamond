# Fonts

Retro Diamond uses self-hosted fonts via npm packages:

- `@fontsource/press-start-2p` — display / pixel headings
- `@fontsource/chakra-petch` — UI body text

Imports live in `src/styles.css`. To swap in local files instead, place `.woff2` assets here and update the CSS `@font-face` rules.
